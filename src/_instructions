    To Run :
        cmake -S . -B build         # create the build directory
        cd build                    # go inside the build directory
        make                        # build the code (compiling)
        ./querk ../_input.qrk       # run the input file (compiling input file)
        ./out                       # run the executable
        echo $?                     # check output
